

// The edit page reuses the same component as the create page.
// It expects an `id` query parameter to switch to edit mode (handled in the create page).
export { default } from '../create/page.jsx';
